# 后端全量功能 QA 问题报告

- 测试时间：2026-09-25
- 测试范围：认证 / 用户资料 / 签到 / 店铺 / 优惠券 / 秒杀（含并发与 Kafka 消费）/ 动态 / 点赞 / 关注 / feed / 上传 / RAG（11 接口）/ 安全边界
- 测试方式：`/tmp/qa.py` 全量脚本实测（58 项断言通过、5 项失败），复现均基于线上 `127.0.0.1:8081`
- 结论：**发现 6 个问题（5 个功能缺陷 + 1 个设计缺陷），已全部修复并复测**

## 问题清单与修复

### B1 店铺详情完全不可访问（严重）

- **现象**：`GET /shop/{id}` 对任何店铺都返回 `{"success":false,"errorMsg":"店铺不存在！"}`，前端店铺详情页不可用。
- **复现**：`curl /shop/1`（库中明明有 14 家店）。
- **根因**：详情只读 Redis 逻辑过期缓存，缓存未命中时直接返回失败、**缺少回源 DB 逻辑**；缓存从未被预热（原 Java 版依赖手工测试预热，Go 重写保留了该行为）。
- **影响**：店铺详情、附近店铺（后续 GEO）全链路不可用。
- **修复**：缓存未命中 → 回源数据库并写入逻辑过期缓存返回；DB 不存在 → 写入 2 分钟空值缓存（防穿透）并提示不存在；解析失败的脏缓存删除后回源。
- **状态**：✅ 已修复

### B2 附近店铺搜索（GEO）永远为空（严重）

- **现象**：`GET /shop/of/type?...&x=&y=` 返回空数组，即使库中有带坐标店铺。
- **根因**：`shop:geo:{typeId}` 索引从未写入；店铺新增/更新时未维护 GEO，也没有懒回填。
- **修复**：新增/更新店铺时 GEOADD（更新时同步旧类型集合清理）；查询时若 GEO 集合为空则从 DB 批量回填该类型带坐标店铺后再搜索。
- **状态**：✅ 已修复

### B3 图片上传失败 & 上传后可访问性（严重，三处子问题）

- **B3a 上传接口 500**：容器以非 root（uid 10001）运行，挂载卷 `/data/uploads` 属主为 root → 无写权限。
  - 修复：镜像内预建 `/data/uploads` 并 chown；部署脚本对已存在卷执行一次 chown。
- **B3b 图片 404**：前端把返回路径拼成 `/imgs/blogs/...` 由 nginx 静态服务，而后端写入 `/data/uploads/blogs/...`，两处不通。
  - 修复：compose 把上传卷只读挂载进 nginx，新增 `location /imgs/blogs/` 别名到卷内目录。
- **B3c 路径穿越（安全）**：`GET /upload/blog/delete?name=../../etc/hostname` 可删除上传目录之外的任意文件（原 Java 同款问题）。
  - 修复：删除前做路径规范化与包含性校验，越界返回「错误的文件名称」。
- **状态**：✅ 已修复

### B4 `POST /voucher`（普通券）错误地走秒杀逻辑（设计缺陷）

- **现象**：普通券接口与秒杀券接口完全同逻辑：都插入 `tb_seckill_voucher` 并预载库存；不带 stock 发布普通券直接 500（原 Java 遗留下的实现错误，被行为等价重写保留）。
- **正确行为**：`POST /voucher` 只插入普通券（`tb_voucher`）；`POST /voucher/seckill` 才建秒杀券（双表 + 库存预载，stock 必填）。
- **修复**：拆分两个服务方法与路由；同步更新 OpenSpec seckill spec。
- **状态**：✅ 已修复

### B5 删除知识库残留评估/问答数据（轻微）

- **现象**：`DELETE /rag/kb/{id}` 只级联清理切片/文档/任务，`rag_eval_run`、`rag_eval_case`、`rag_chat_log` 残留脏数据。
- **修复**：删除知识库事务内级联清理三类记录。
- **状态**：✅ 已修复

### B7 「我的动态」超过一页后看不到最新动态（严重）

- **现象**：用户动态超过 10 条后，`GET /blog/of/me` 第一页返回的是**最旧**的 10 条，刚发布的动态不出现（`/blog/of/user` 同样）。
- **根因**：分页查询缺少 `ORDER BY`（原 Java 版同样未排序），数据量超过一页后顺序退化。
- **修复**：按 `id DESC` 排序；新增回归测试（15 条数据断言第一页为 15..6）。
- **状态**：✅ 已修复（QA 复测触发：日志显示多条动态的朋友出现该问题）

### B8 验证码留空登录报「服务器异常」（严重）

- **现象**：登录页验证码留空直接点登录 → `{"success":false,"errorMsg":"服务器异常"}`。
- **根因**：前端未填写验证码时请求体不含 `code` 字段，而 back-end 登录 handler 将 `code == nil` 判为参数错误并返回「服务器异常」（为关闭验证码校验而放宽前端校验后暴露）。
- **修复**：`code` 允许缺省（按空串处理）；缺手机号提示「手机号不能为空」；关闭校验时任意验证码/空验证码均可登录，恢复校验时缺省 code 正常提示「验证码不一致」。
- **状态**：✅ 已修复

### B9 前端「死按钮」与 RAG 页面缺失（严重，前端专项 QA）

用 Playwright 无头浏览器把所有交互真实点了一遍（脚本 `scripts/ui_click_test.js`），发现并修复：

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| B9a | 首页「今日签到 +5 积分」卡片点了没反应 | 纯展示元素没有绑定事件（后端早有 `/user/sign`） | 绑定点击→签到→显示连续天数 |
| B9b | 首页搜索框无任何反应 | 输入框没有绑定回车/按钮事件 | 回车/图标触发搜索并跳转店铺列表（支持 `search` 参数） |
| B9c | footer「附近/互动」两个 tab 点了没反应 | `toPage()` 只处理 0/1/4 | 附近→店铺列表；互动→替换为「知识库」并新增 RAG 页面入口 |
| B9d | 店铺列表「距离/热度/评分」排序无效且列表叠加 | 后端无排序参数；前端排序未重置列表 | 后端支持 `sortBy`；前端排序重置页码/列表，触底分页加 `hasMore` 保护 |
| B9e | 资料编辑页所有行点不动、没有保存按钮 | 原页面为纯展示骨架 | 行点击弹窗编辑（昵称/头像/介绍/性别/校区/生日）+ 保存按钮（配套新增两个后端接口） |
| B9f | 发布动态不选店铺时报错无提示 | `this.selectedShop.id` 直接抛异常 | 增加店铺/标题/内容校验与友好提示 |
| B9g | 删除动态图片后文件仍残留 | 前端带 `/imgs` 前缀调用删除接口 | 去掉前缀后调用 |
| B9h | **完全没有 RAG 页面**（11 个接口无 UI） | 之前只实现了 API | 新增 `rag.html`：知识库选择/新建、文档上传与状态轮询、带引用角标的问答（可点开引用详情）、RAGAS 评估面板 |
| B9i | 种子动态图片 404（多处） | 数据引用了未随仓库分发的老图片 | 图片 `onerror` 兜底 + 补占位图 |

### B10 修改昵称后当前会话仍显示旧昵称（严重）

- **现象**：资料编辑保存成功后，`/user/me` 与页面仍显示旧昵称（数据库已更新）。
- **根因**：登录态是 Redis Hash 快照，更新 `tb_user` 后没有同步会话缓存。
- **修复**：保存资料后同步刷新 Redis 会话字段（含头像）；回归测试覆盖。
- **状态**：✅ 已修复

### B11 用户详情不存在时创建失败（严重）

- **现象**：首次保存「个人介绍/校区/生日」返回「服务器异常」。
- **根因**：GORM 以全字段 INSERT，`create_time` 的 NULL 覆盖了表默认值（MySQL 严格模式拒绝）。
- **修复**：创建时 `Select("user_id")` 仅插入主键，时间戳走数据库默认值；回归测试覆盖。
- **状态**：✅ 已修复

### B6 静态代码审查发现（非 QA 失败项）

- `POST /user/logout` 恒返回「功能未完成」：**保留原行为**（前端不依赖）。
- `POST /blog` 不关联店铺时会 500（`shop_id` NOT NULL）：与前端「必须选择服务点」的交互一致，暂按原行为保留。
- 热门榜/详情逐条查作者（N+1 查询）：数据量小，暂不优化，已记录。

## 复测结果

- 修复后重跑全量 QA：**68/68 全部通过**（脚本已纳入仓库：`scripts/qa_full.py`，可随时回归）
- 最终修复清单：B1 店铺回源 / B2 GEO 索引 / B3 上传权限+静态路径+路径穿越 / B4 券拆分 / B5 级联清理 / B7 动态排序 / B8 登录缺省 code
- 修复过程中补充确认的两个"伪问题"（非缺陷）：
  1. **Kafka 订单落库延迟**：消费者重启后的 group rebalance 需要数秒，首轮 QA 固定等待 3 秒导致误报；带轮询复测为 **1 秒内落库、库存正确扣减**。
  2. **GEO 首轮空结果**：QA 使用的查询坐标距店铺 12km（超出 5000m 半径），修正坐标后正常命中。
- 路径穿越的修复策略为"路径锚定到上传目录"：越界输入不会删除目录外文件（QA 通过容器内诱饵文件验证），而非返回错误——对不存在的路径仍按原行为静默成功。
