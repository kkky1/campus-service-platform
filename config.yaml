server:
  port: 8081
mysql:
  dsn: root:123456@tcp(127.0.0.1:3306)/hmdp?charset=utf8mb4&parseTime=True&loc=Local
redis:
  addr: 127.0.0.1:6379
  password: ""
kafka:
  brokers:
    - localhost:9092
upload:
  dir: ./data/uploads
rag:
  chunk:
    token_num: 256
    overlap_percent: 10
  embedding:
    provider: local
    dim: 384
  llm:
    base_url: https://api.deepseek.com
    model: deepseek-chat
  retrieval:
    top_k: 8
    vector_weight: 0.7
    keyword_weight: 0.3
login:
  # 临时：登录不校验短信验证码（前端可留空验证码）。生产环境请设为 false。
  # 也可用环境变量 LOGIN_SKIP_CODE=true/false 覆盖
  skip_code: true
